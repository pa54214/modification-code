 date($this->customlib->getSchoolDateFormat(), strtotime($value['admission_date']))



$this->customlib->dateformat($student->admission_date);


use for database 
feetype.code,feetype.type,feetype.description

date('Y-m-d', strtotime($result[$i]['date_of_birth']));
