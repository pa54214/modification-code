<style type="text/css">
    /* Global Page Setup for Crisp 3x3 Grid A4 Printing */
    @page {
        size: A4 portrait;
        margin: 5mm 4mm; /* Compact printable margin areas */
    }
    
    * { 
        margin: 0; 
        padding: 0; 
        box-sizing: border-box; 
        -webkit-print-color-adjust: exact; 
        print-color-adjust: exact;
    }
    
    body {
        font-family: 'Arial', sans-serif;
        background: #fff;
    }

    /* Print Grid Wrapper */
    .id-card-grid {
        width: 100%;
        text-align: center;
        font-size: 0; /* Clears hidden inline spacing block anomalies */
    }

    /* ==========================================
       VERTICAL CARD STYLES (Optimized 3x3 Form)
       ========================================== */
    .vertical-card-container {
        display: inline-block;
        width: 64mm; 
        height: 88mm; /* Reduced to guarantee 3 rows fit safely inside A4 297mm height */
        margin: 2mm 1.5mm; /* Tight layout gaps */
        vertical-align: top;
        font-size: 10px; 
        color: #000;
        border: 1px solid #bbb;
        border-radius: 5px;
        overflow: hidden;
        background: #ffffff;
        position: relative;
        text-align: left; 
    }
    
    .v-header {
        padding: 4px 4px;
        text-align: center;
        color: #fff;
    }
    .v-logo {
        vertical-align: middle;
        margin-right: 3px;
        width: 18px;
        height: 18px;
        object-fit: contain;
    }
    .v-school-name {
        font-size: 10px;
        font-weight: bold;
        line-height: 1.1;
        display: inline-block;
        vertical-align: middle;
    }
    .v-school-address {
        font-size: 7.5px;
        opacity: 0.9;
        line-height: 1.1;
        margin-top: 1px;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }
    .v-body {
        padding: 4px 6px;
        text-align: center;
    }
    .v-avatar {
        width: 44px;
        height: 44px;
        object-fit: cover;
        border-radius: 4px;
        margin: 0 auto 3px auto;
        display: block;
    }
    .v-name {
        font-size: 10.5px;
        text-transform: uppercase;
        font-weight: bold;
        color: #111;
        margin-bottom: 3px;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
        text-align: center;
    }
    .v-list {
        list-style: none;
        padding: 0;
        margin: 0;
    }
    .v-list li {
        text-align: left;
        font-size: 8.5px;
        padding: 1.5px 0;
        border-bottom: 1px dashed #e5e5e5;
        line-height: 1.1;
    }
    .v-list li:last-child {
        border-bottom: none;
    }
    .v-list li span {
        font-weight: bold;
        float: right;
        width: 60%;
        text-align: right;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
        color: #222;
    }
    .v-footer {
        position: absolute;
        bottom: 3px;
        left: 0;
        width: 100%;
        text-align: center;
        padding: 0 6px;
    }
    .v-sign-wrap {
        display: block;
        width: 100%;
        text-align: center;
    }
    .v-sign {
        max-height: 16px;
        max-width: 70px;
        display: inline-block;
    }
    .v-barcode-wrap {
        display: block;
        width: 100%;
        text-align: center;
        margin-top: 1px;
    }
    .v-barcode {
        max-height: 12px;
        max-width: 85px;
        display: inline-block;
    }

    /* ==========================================
       HORIZONTAL CARD STYLES
       ========================================== */
    .horizontal-card-container {
        display: inline-block;
        width: 85.6mm;
        height: 54mm;
        margin: 3mm 2mm;
        vertical-align: top;
        font-size: 11px;
        color: #000;
        border: 1px solid #bbb;
        border-radius: 6px;
        overflow: hidden;
        background: #fdfdfd;
        position: relative;
        text-align: left;
    }
    .h-bg {
        position: absolute;
        left: 50%;
        top: 50%;
        transform: translate(-50%, -50%);
        width: 100px;
        height: 100px;
        opacity: 0.06;
        object-fit: contain;
        z-index: 0;
        pointer-events: none;
    }
    .h-header {
        padding: 4px 8px;
        color: #fff;
        position: relative;
        z-index: 1;
    }
    .h-title-bar {
        background: #e2e2e2;
        font-size: 8px;
        font-weight: bold;
        text-transform: uppercase;
        text-align: center;
        padding: 1px 0;
        position: relative;
        z-index: 1;
    }
    .h-body {
        padding: 4px 6px;
        position: relative;
        z-index: 1;
    }
    .h-left {
        width: 24%;
        float: left;
        text-align: center;
    }
    .h-avatar {
        width: 48px;
        height: 48px;
        object-fit: cover;
        border-radius: 3px;
        margin-bottom: 3px;
    }
    .h-barcode {
        max-height: 14px;
        max-width: 100%;
    }
    .h-right {
        width: 74%;
        float: right;
        padding-left: 4px;
    }
    .h-list {
        list-style: none;
    }
    .h-list li {
        font-size: 8.5px;
        padding: 1px 0;
        border-bottom: 1px dashed #e2e2e2;
    }
    .h-list li span {
        font-weight: bold;
        float: right;
        width: 64%;
        text-align: right;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }
    .h-footer {
        position: absolute;
        bottom: 3px;
        right: 8px;
        text-align: right;
        z-index: 1;
    }
    .h-sign {
        max-height: 16px;
        max-width: 55px;
        display: block;
        margin-left: auto;
    }
    .h-sign-txt {
        font-size: 6.5px;
        color: #555;
    }

    .clearfix::after {
        content: "";
        clear: both;
        display: table;
    }
    .page-break {
        page-break-after: always;
        clear: both;
    }
</style>

<?php
$school = $sch_setting[0];
$counter = 0;
?>

<div class="id-card-grid">
<?php 
foreach ($students as $student) {
    $counter++;
    
    $student_img = (!empty($student->image)) ? $student->image : (($student->gender == 'Female') ? "uploads/student_images/default_female.jpg" : "uploads/student_images/default_male.jpg");
    
    $dob = "";
    if ((!empty($student->dob)) && ($student->dob != "0000-00-00")) {
        $dob = date($this->customlib->getSchoolDateFormat(), $this->customlib->dateYYYYMMDDtoStrtotime($student->dob));
    }
    $fullname = $this->customlib->getFullName($student->firstname, $student->middlename, $student->lastname, $sch_settingdata->middlename, $sch_settingdata->lastname);

    if ($id_card[0]->enable_vertical_card) {
        /* =======================================================
           VERTICAL LAYOUT (3 columns x 3 rows grid safe)
           ======================================================= */
        ?>
        <div class="vertical-card-container">
            <div class="v-header" style="background: <?php echo $id_card[0]->header_color; ?>;">
                <div>
                    <img class="v-logo" src="<?php echo $this->media_storage->getImageURL('uploads/student_id_card/logo/'.$id_card[0]->logo); ?>">
                    <span class="v-school-name"><?php echo $id_card[0]->school_name; ?></span>
                </div>
                <div class="v-school-address"><?php echo $id_card[0]->school_address; ?></div>
            </div>
            
            <div class="v-body">
                <img class="v-avatar" src="<?php echo $this->media_storage->getImageURL($student_img); ?>" style="border: 1.5px solid <?php echo $id_card[0]->header_color; ?>;">
                <div class="v-name"><?php echo $fullname; ?></div>
                
                <ul class="v-list">
                    <?php if ($id_card[0]->enable_admission_no == 1) { ?>
                        <li>Admission No: <span><?php echo $student->admission_no; ?></span></li>
                    <?php } ?>
                    <?php if ($id_card[0]->enable_class == 1) { ?>
                        <li>Class: <span><?php echo $student->class . ' - ' . $student->section; ?></span></li>
                    <?php } ?>
                    <?php if ($id_card[0]->enable_fathers_name == 1) { ?>
                        <li>Father's Name: <span><?php echo $student->father_name; ?></span></li>
                    <?php } ?>
                    <?php if ($id_card[0]->enable_phone == 1) { ?>
                        <li>Phone: <span><?php echo $student->mobileno; ?></span></li>
                    <?php } ?>
                    <?php if ($id_card[0]->enable_dob == 1 && !empty($dob)) { ?>
                        <li>D.O.B: <span><?php echo $dob; ?></span></li>
                    <?php } ?>
                    <?php if ($id_card[0]->enable_blood_group == 1) { ?>
                        <li style="color: #c9302c;">Blood Group: <span><?php echo $student->blood_group; ?></span></li>
                    <?php } ?>
                </ul>
            </div>
            
            <div class="v-footer">
                <div class="v-sign-wrap">
                    <img class="v-sign" src="<?php echo $this->media_storage->getImageURL('uploads/student_id_card/signature/'.$id_card[0]->sign_image); ?>" />
                </div>
                <?php if ($id_card[0]->enable_student_barcode == 1) { ?>
                    <div class="v-barcode-wrap">
                        <img class="v-barcode" src="<?php echo $this->media_storage->getImageURL($student->barcode); ?>" />
                    </div>
                <?php } ?>
            </div>
        </div>
        <?php
    } else {
        /* =======================================================
           HORIZONTAL LAYOUT
           ======================================================= */
        ?>
        <div class="horizontal-card-container">
            <img class="h-bg" src="<?php echo $this->media_storage->getImageURL('uploads/student_id_card/background/' . $id_card[0]->background); ?>" />
            
            <div class="h-header" style="background: <?php echo $id_card[0]->header_color; ?>;">
                <img src="<?php echo $this->media_storage->getImageURL('uploads/student_id_card/logo/' . $id_card[0]->logo); ?>" width="18" height="18" style="vertical-align: middle; margin-right:3px;" />
                <span class="v-school-name" style="font-size:10px;"><?php echo $id_card[0]->school_name; ?></span>
            </div>
            
            <div class="h-title-bar" style="color: <?php echo $id_card[0]->header_color; ?>;">
                <?php echo $id_card[0]->title; ?>
            </div>
            
            <div class="h-body clearfix">
                <div class="h-left">
                    <img class="h-avatar" src="<?php echo $this->media_storage->getImageURL($student_img); ?>" style="border: 1px solid <?php echo $id_card[0]->header_color; ?>;" />
                    <?php if ($id_card[0]->enable_student_barcode == 1) { ?>
                        <img class="h-barcode" src="<?php echo $this->media_storage->getImageURL($student->barcode); ?>" />
                    <?php } ?>
                </div>
                
                <div class="h-right">
                    <ul class="h-list">
                        <?php if ($id_card[0]->enable_student_name == 1) { ?>
                            <li style="font-weight: bold; border-bottom: 1px solid #bbb; font-size:9.5px;"><?php echo $fullname; ?></li>
                        <?php } ?>
                        <?php if ($id_card[0]->enable_admission_no == 1) { ?>
                            <li>Adm No: <span><?php echo $student->admission_no; ?></span></li>
                        <?php } ?>
                        <?php if ($id_card[0]->enable_class == 1) { ?>
                            <li>Class: <span><?php echo $student->class . ' - ' . $student->section; ?></span></li>
                        <?php } ?>
                        <?php if ($id_card[0]->enable_fathers_name == 1) { ?>
                            <li>Father: <span><?php echo $student->father_name; ?></span></li>
                        <?php } ?>
                        <?php if ($id_card[0]->enable_phone == 1) { ?>
                            <li>Phone: <span><?php echo $student->mobileno; ?></span></li>
                        <?php } ?>
                        <?php if ($id_card[0]->enable_dob == 1 && !empty($dob)) { ?>
                            <li>DOB: <span><?php echo $dob; ?></span></li>
                        <?php } ?>
                    </ul>
                </div>
            </div>
            
            <div class="h-footer">
                <img class="h-sign" src="<?php echo $this->media_storage->getImageURL('uploads/student_id_card/signature/' . $id_card[0]->sign_image); ?>" />
                <span class="h-sign-txt">Authorized Signatory</span>
            </div>
        </div>
        <?php
    }

    // Splits clean page breaks precisely after 9 cards (vertical) or 8 cards (horizontal)
    $cards_per_page = ($id_card[0]->enable_vertical_card) ? 9 : 8;
    if ($counter % $cards_per_page == 0) {
        echo '</div><div class="page-break"></div><div class="id-card-grid">';
    }
}
?>
</div>
